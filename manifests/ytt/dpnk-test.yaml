#@ app_name = "dpnk-test"
#@ image = "439267276348970/rtwbb-backend-test:2026.0"
#@ config_map = "dpnk-test-env"
#@ secret_map = "dpnk-test-access"
#@ load("dpnk.lib.yml", "web")
--- #@ web(app_name, image, config_map, secret_map, 1, 1)
